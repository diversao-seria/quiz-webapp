
//Add question
$('#questions').append($('<div id="question">').html("<%= escape_javascript(render :partial => 'question')%>").hide().fadeIn())

//Remove question
$('.remove-question').click(function(){
  if (!confirm('Tem certeza que deseja remover a questão?')) { return false }

  $(this).parent().parent().parent().fadeOut("normal", function() { 
    $(this).remove() 
    } );
  }
);
